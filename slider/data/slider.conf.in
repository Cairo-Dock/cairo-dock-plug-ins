#!en;@VERSION@

#[gtk-about]
[Icon]
#F[Desired icon size for this applet]
frame1 = 
#i width :
width = 32
#i height :
height = 32

#F
frame1_ = 

#s Name of the icon as it will appear in its label in the dock :
name = Slider

#E[] Name of the dock it belongs to:
dock name = 

order=

#[gtk-convert]
[Desklet]
#i[48;512] Desklet's dimension (width x height) :
#{Depending on your WindowManager, you can resize it with ALT + middle_click or ALT + left_click for exemple.}
width=92
#i[48;512] ...
height=92

#i Desklet's position (x ; y) :
#{Depending on your WindowManager, you can move it with ALT + left_click}
x position=0
#i ...
y position=0

#b Is detached from the dock ?
initially detached=false

#b Keep below other windows ?
keep below=false
#b Keep above other windows ?
keep above=false
#b Keep on widget layer ?
#{for CompizFusion's "widget layer", set behaviour in Compiz to: (name=cairo-dock & type=utility)}
on widget layer=false
#b Lock position ?
#{If locked, the desklet can't be moved by simply dragging it with the left mouse button. Of course you can still move it with ALT + left_click.}
locked = false

#[gtk-preferences]
[Configuration]
#F[Directory;gtk-file]
frame_td=
#D Directory of the images :
directory= /
#b Scan in sub direcotries ?
#{Not yet implemented.}
sub directories=false
#b Randomly browse files ?
random = false

#F[Behaviour;gtk-execute]
frame_ops=
#i[1;500] Sliding delay :
#{in seconds.}
slide time=30
#r[play/pause;open image] Action when applet is clicked :
click=0
#r[none;fade;blank fade;fade in out;side kick;diaporama;grow up;shrink down;random] Effect while changing image :
change animation=1

#F[Display;gtk-dnd-multiple]
frame_disp=
#b Keep widht/height ratio ?
#{Gives a better image rendering.}
no stretch=true
#b Force the image to fill the icon/desklet ?
#{If the image is too small, it will be zoomed in.}
fill icon = false
#c4[0;1] Choose a background color :
#{Set the transparency channel to 0 if you just don't want to have a background.}
background color = 1.0; 1.0; 1.0; 1.0

#F[Background & Reflection;gtk-orientation-portrait]
frame_bg =
#S Background image :
#{It's an image that will be displayed below the slide, like a frame for exemple. Let it empty to use the default one.}
frame =
#e[0;1] Background tansparency :
#{Set it to zero if you don't want to use a background}
frame alpha = 1
#i Background offset :
#{It's typically the width of your frame in pixels.}
frame offset = 5
#S Reflection image :
#{It's an image that will be displayed above the slide, like a reflect for exemple. Let it empty to use the default one.}
reflect =
#e[0;1] Reflection tansparency :
#{Set it to zero if you dont don't want to use a foreground}
reflect alpha = 1
